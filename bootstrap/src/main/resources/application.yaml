spring:
  application:
    name: agp-protocol
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/RisingHelpDeskDB?createDatabaseIfNotExist=true
    username: proxysql_user
    password: secret