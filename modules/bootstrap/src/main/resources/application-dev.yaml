server:
  port: 8001
spring:
  data:
    redis:
      host: redis
      port: 6379
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  application:
    name: agp-protocol
  datasource:
    url: jdbc:mysql://localhost:3306/RisingHelpDeskDB?createDatabaseIfNotExist=true
    username: root
    password:
logging:
  level:
    org.mariadb.jdbc: DEBUG
    com.zaxxer.hikari: DEBUG