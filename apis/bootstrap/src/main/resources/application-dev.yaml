spring:
  jpa:
    hibernate:
      ddl-auto: create-drop          # ou "create"
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  application:
    name: agp-protocol

logging:
  level:
    com.zaxxer.hikari: DEBUG