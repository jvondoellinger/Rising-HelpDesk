spring:
  jpa:
    hibernate:
      ddl-auto: create
  application:
    name: agp-protocol
  datasource:
    url: jdbc:mysql://localhost:6033/RisingHelpDeskDB?createDatabaseIfNotExist=true&transactionIsolation=READ_COMMITTED
    username: proxysql_user
    password: secret123
logging:
  level:
    org.mariadb.jdbc: DEBUG
    com.zaxxer.hikari: DEBUG